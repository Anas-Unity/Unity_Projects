using UnityEngine;

public class CameraMovement : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    public GameObject playerMovement;       //this variable is used to attach player with camera


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
       // transform.position = playerMovement.transform.position;     // this function will change camera position with player


    }
}
